{
  "name": "product-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix product-backend",
    "frontend": "npm run dev --prefix my-react-app",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.0"
  }
}